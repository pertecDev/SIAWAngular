/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ExceltoexcelService } from './exceltoexcel.service';

describe('Service: Exceltoexcel', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ExceltoexcelService]
    });
  });

  it('should ...', inject([ExceltoexcelService], (service: ExceltoexcelService) => {
    expect(service).toBeTruthy();
  }));
});
